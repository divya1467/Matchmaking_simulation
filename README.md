# Matchmaking Game

A small project that simulates matchmaking between players based on configurable criteria (skill, preferences, availability).

## Features
- Match players into games/sessions using pluggable algorithms
- Support for constraints (skill range, Game mode)
- Simple CLI/API to create players, run matching, and inspect results